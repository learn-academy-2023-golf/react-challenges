import React, {useState} from "react";

const PreviousRolls = (props) => {
    
return (
    <div className= "history">
       <h4>History: {props.oldNums}</h4>
    </div>

)
}

export default PreviousRolls